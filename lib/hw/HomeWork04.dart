import 'dart:math';

import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';
/*
void main() {
  var r = Random();
  String count = r.nextInt(100).toString();

  var textWidget = Text(
    '$count',
    style: TextStyle(
        fontSize:  150.0,
        color: Colors.cyan),
      textDirection: TextDirection.ltr);
  var centerWidget = Center(child: textWidget);
  runApp(centerWidget);


/*
  var r = Random();
  String num = r.nextInt(100).toString();

  var textWidget = Text(
      '$num',
      style: TextStyle(
          fontSize:  150.0,
          color: Colors.cyan),
      textDirection: TextDirection.ltr);

  var centerWidget = Center(child: textWidget);
  runApp(centerWidget);

  var textWidget = Text(
      'Hello Flutter',
      textDirection: TextDirection.ltr
  );
  var centerWidget = Center(child: textWidget);

  runApp(centerWidget);*/
}*/
